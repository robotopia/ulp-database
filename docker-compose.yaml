version: "3.9"

# Variables passed to the containers
networks:
  ulp-network:
    name: ulp-network

volumes:
  ulp-data:

services:
  postgres:
    hostname: ${DBHOST}
    container_name: postgres
    build: postgres
    ports:
      - "8001:${DBPORT}"
    restart: always
    environment:
      DBNAME: ${DBNAME}
      DBUSER: ${DBUSER}
      DBPASS: ${DBPASS}
      POSTGRES_PASSWORD: ${DBPASS}
    networks:
      - ulp-network
    volumes:
      - ulp-data:/var/lib/postgres/data

  django:
    hostname: django
    container_name: django
    build: ulp-webapp
    restart: always
    depends_on:
      - postgres
    environment:
      DBNAME: ${DBNAME}
      DBUSER: ${DBUSER}
      DBPASS: ${DBPASS}
      DBHOST: ${DBHOST}
      DBPORT: ${DBPORT}
    networks:
      - ulp-network
    volumes:
      - ulp-data:/var/lib/postgres/data

# nginx:
#   hostname: nginx
#   container_name: nginx
#   build: nginx
#   restart: always
#   networks:
#     - ulp-network
#   volumes:
#     - uwsgi_data:/tmp/uwsgi/
#   ports:
#     - "80:80"
#   depends_on:
#     - django
